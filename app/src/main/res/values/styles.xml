<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DetailScreenInfoTextBox">
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Medium</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <style name="DetailScreenInfoTextBox.AdjacentToImage" parent="DetailScreenInfoTextBox">
        <item name="android:textSize">@dimen/next_to_image_text_size</item>
    </style>

    <style name="DetailScreenLabelTextBox">
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Caption</item>
        <item name="android:textColor">@android:color/holo_red_dark</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>
</resources>
